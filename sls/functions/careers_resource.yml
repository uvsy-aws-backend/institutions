careers_resource:
  name: ${self:provider.stage}-${self:service}-careers_resource
  handler: app.uvsy.InstitutionAPI
  environment:
    DB_DRIVER: ${self:custom.dbDriver}
    DB_PROTOCOL: ${self:custom.dbProtocol}
    DB_PORT: ${self:custom.dbPort}
    DB_NAME: ${self:custom.dbName}
    DB_HOST: ${self:custom.dbHost}
    DB_USERNAME: ${self:custom.dbUser}
    DB_PASSWORD: ${self:custom.dbPassword}
  events:
    - http:
        path: /careers/{id}
        method: GET
        cors: true
        request:
          parameters:
            paths:
              id: true
    - http:
        path: /careers/{id}
        method: PUT
        cors: true
        request:
          parameters:
            paths:
              id: true
    - http:
        path: /careers/{id}
        method: DELETE
        cors: true
        request:
          parameters:
            paths:
              id: true
    - http:
        path: /careers/{id}/activate
        method: POST
        cors: true
        request:
          parameters:
            paths:
              id: true
    - http:
        path: /careers/{id}/programs
        method: POST
        cors: true
        request:
          parameters:
            paths:
              id: true
    - http:
        path: /careers/{id}/programs
        method: GET
        cors: true
        request:
          parameters:
            paths:
              id: true
