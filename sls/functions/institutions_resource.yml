institutions_resource:
  name: ${self:provider.stage}-${self:service}-institutions_resource
  handler: app.uvsy.InstitutionAPI
  environment:
    DB_DRIVER: ${self:custom.dbDriver}
    DB_PROTOCOL: ${self:custom.dbProtocol}
    DB_PORT: ${self:custom.dbPort}
    DB_NAME: ${self:custom.dbName}
    DB_HOST: ${self:custom.dbHost}
    DB_USERNAME: ${self:custom.dbUser}
    DB_PASSWORD: ${self:custom.dbPassword}
  events:
    - http:
        path: /institutions
        method: GET
    - http:
        path: /institutions
        method: POST
    - http:
        path: /institutions/{id}
        method: GET
        request:
          parameters:
            paths:
              id: true
    - http:
        path: /institutions/{id}
        method: PUT
        request:
          parameters:
            paths:
              id: true
    - http:
        path: /institutions/{id}
        method: DELETE
        request:
          parameters:
            paths:
              id: true
    - http:
        path: /institutions/{id}/activate
        method: POST
        request:
          parameters:
            paths:
              id: true
    - http:
        path: /institutions/{id}/careers
        method: POST
        request:
          parameters:
            paths:
              id: true
    - http:
        path: /institutions/{id}/careers
        method: GET
        request:
          parameters:
            paths:
              id: true
