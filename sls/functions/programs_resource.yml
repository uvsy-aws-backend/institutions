programs_resource:
  name: ${self:provider.stage}-${self:service}-programs_resource
  handler: app.uvsy.InstitutionAPI
  environment:
    DB_DRIVER: ${self:custom.dbDriver}
    DB_PROTOCOL: ${self:custom.dbProtocol}
    DB_PORT: ${self:custom.dbPort}
    DB_NAME: ${self:custom.dbName}
    DB_HOST: ${self:custom.dbHost}
    DB_USERNAME: ${self:custom.dbUser}
    DB_PASSWORD: ${self:custom.dbPassword}
  events:
    - http:
        path: /programs/{id}
        method: GET
        cors: true
        request:
          parameters:
            paths:
              id: true
    - http:
        path: /programs/{id}
        method: PUT
        cors: true
        request:
          parameters:
            paths:
              id: true
    - http:
        path: /programs/{id}
        method: DELETE
        cors: true
        request:
          parameters:
            paths:
              id: true
    - http:
        path: /programs/{id}/activate
        method: POST
        cors: true
        request:
          parameters:
            paths:
              id: true
    - http:
        path: /programs/{id}/subjects
        method: GET
        cors: true
        request:
          parameters:
            paths:
              id: true
    - http:
        path: /programs/{id}/subjects
        method: POST
        cors: true
        request:
          parameters:
            paths:
              id: true
    - http:
        path: /programs/{id}/commissions
        method: GET
        cors: true
        request:
          parameters:
            paths:
              id: true
    - http:
        path: /programs/{id}/commissions
        method: POST
        cors: true
        request:
          parameters:
            paths:
              id: true